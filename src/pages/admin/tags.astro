---
import Layout from "@/layouts/Layout.astro";
import TagManager from "@/components/react/admin/TagManager";
import { SITE } from "@/config";

export const prerender = false;
---

<Layout title={`Tags | ${SITE.title}`}>
  <div style="max-width: 56rem; margin: 0 auto; padding: 2rem 1rem;">
    <h1 style="font-size: 1.5rem; font-weight: 700; margin-bottom: 1.5rem;">
      <a href="/" style="color: var(--foreground); text-decoration: none;">{SITE.title}</a>
      <span style="color: var(--accent);"> / Admin / Tags</span>
    </h1>
    <div style="display: flex; gap: 1rem; margin-bottom: 2rem;">
      <a href="/admin" style="color: var(--foreground); opacity: 0.7;">Posts</a>
      <a href="/admin/tags" style="color: var(--accent); font-weight: 500;">Tags</a>
    </div>
    <TagManager client:load />
  </div>
</Layout>
