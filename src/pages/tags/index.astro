---
export const prerender = false;
import Main from "@/layouts/Main.astro";
import Layout from "@/layouts/Layout.astro";
import Tag from "@/components/Tag.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import { getRuntimePosts, getRuntimeTags } from "@/utils/runtimePosts";
import { SITE } from "@/config";

Astro.response.headers.set("Cache-Control", "public, max-age=0, s-maxage=30");

let tags = getRuntimeTags(getRuntimePosts());
---

<Layout title={`Tags | ${SITE.title}`}>
  <Header />
  <Main pageTitle="Tags" pageDesc="All the tags used in posts.">
    <ul>
      {tags.map(({ tag, tagName }) => <Tag {tag} {tagName} size="lg" />)}
    </ul>
  </Main>
  <Footer />
</Layout>
